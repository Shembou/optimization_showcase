syntax = "proto3";
package greeter;

service Greeter {
    rpc HelloRPC (HelloRequest) returns (HelloReply);
}

message HelloRequest {
    string name = 1;
}
message HelloReply {
    string message = 1;
}